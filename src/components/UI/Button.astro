---
const props = Astro.props;
---

<button
  class={`text-md w-72 h-auto bg-pastel-pink border-0 shadow-lg rounded-lg text-white p-2 hover:bg-pastel-red focus:outline-none focus:ring-2 focus:ring-pastel-pink focus:ring-offset-2 focus:ring-offset-white transition duration-300 ease-in-out ${props.className}`}
  {...props}
>
  <slot />
</button>
